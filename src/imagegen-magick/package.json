{
  "name": "imagegen-magick-src",
  "version": "1.0.0",
  "private": true,
  "description": "imagegen-magick 技能的源码项目（Vite 8 + Rolldown + TypeScript，编译到 skills/）",
  "type": "module",
  "scripts": {
    "dev": "node build.mjs",
    "build": "node build.mjs",
    "build:test": "vite build",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --ext .ts",
    "clean": "rm -rf ../../skills/imagegen-magick/scripts/dist"
  },
  "dependencies": {
    "commander": "^14.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "eslint": "^8.57.0",
    "typescript": "^5.4.0",
    "vite": "^8.0.0",
    "vitest": "^1.4.0"
  },
  "engines": {
    "node": ">=22"
  },
  "keywords": [
    "imagegen",
    "imagemagick",
    "svg",
    "agent-skills"
  ]
}
