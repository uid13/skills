import { defineConfig } from 'unocss'

/**
 * UnoCSS 配置（pp 技能）
 *
 * 使用 UnoCSS 原生类名，不依赖 presetWind3 预设
 * 所有样式类直接使用 UnoCSS 语法
 */
export default defineConfig({
  // 使用 UnoCSS 默认预设（包含基础原子类）
  // 如需更多 Tailwind 兼容类名，可后续添加 presetWind3
})
