# 外部素材获取指南

> Agent 在生成 SVG 时，可通过公共 API 获取图标、插图等素材，融合到设计中。

## Iconify 公共 API

Iconify 提供 275k+ 开源图标，覆盖 200+ 图标集。免费使用，无需 API Key。

**公共 API 地址**：`https://api.iconify.design`

**备用地址**（主站不可用时）：
- `https://api.simplesvg.com`
- `https://api.unisvg.com`

---

### 核心 API

#### 1. 搜索图标

```
GET https://api.iconify.design/search?query={关键词}&limit={数量}
```

**参数**：
- `query`（必填）：搜索关键词，如 `terminal`、`code`、`cloud`
- `limit`（可选）：返回数量，默认 30
- `prefix`（可选）：限定图标集，如 `mdi`、`lucide`

**返回示例**：
```json
{
  "icons": ["mdi:terminal", "mdi:console", "codicon:terminal", ...],
  "total": 156
}
```

**用法**：Agent 根据用户需求的场景搜索关键词，获取图标名列表。

#### 2. 获取 SVG

```
GET https://api.iconify.design/{prefix}/{name}.svg
```

**参数**：
- `prefix`：图标集前缀（如 `mdi`、`lucide`）
- `name`：图标名（如 `terminal`、`cloud`）
- `width`（可选）：SVG 宽度
- `height`（可选）：SVG 高度
- `color`（可选）：图标颜色（如 `%23FF0000` 表示 #FF0000）

**返回**：纯 SVG 字符串，可直接嵌入到生成的 SVG 中。

**示例**：
```
https://api.iconify.design/mdi/terminal.svg?width=48&color=%2358A6FF
```

#### 3. 获取图标 JSON 数据

```
GET https://api.iconify.design/{prefix}.json?icons={name1},{name2}
```

**用途**：需要对图标做二次加工（改颜色、缩放、组合）时，获取 SVG 路径数据。

**返回**：JSON 格式的图标数据（body、width、height 等）。

#### 4. 列出图标集

```
GET https://api.iconify.design/collections
```

**返回**：所有可用图标集的列表（名称、图标数量、作者等）。

#### 5. 列出图标集中的图标

```
GET https://api.iconify.design/{prefix}
```

**返回**：指定图标集中的所有图标名。

---

### 推荐图标集（按优先级）

| 优先级 | 图标集 | prefix | 数量 | 风格 | 适用场景 |
|--------|--------|--------|------|------|---------|
| 1 | Material Design Icons | `mdi` | ~7000+ | Google Material | 通用，最全 |
| 2 | Lucide | `lucide` | ~1500+ | 简洁现代 | 封面、社交媒体 |
| 3 | Codicons | `codicon` | ~400+ | VS Code 风格 | 技术、CLI、开发工具 |
| 4 | Phosphor | `ph` | ~7000+ | 6 种粗细 | 设计感强 |
| 5 | Heroicons | `heroicons` | ~300+ | Tailwind 官方 | 简约 UI |
| 6 | Bootstrap Icons | `bi` | ~2000+ | Bootstrap 风格 | Web 相关 |
| 7 | Ant Design Icons | `ant-design` | ~3000+ | 蚂蚁设计 | 中文场景 |

---

### 图标数量

**每次设计最多使用 2-3 个图标**，避免视觉混乱。图标是装饰元素，不是主角。

| 设计类型 | 推荐图标数 | 说明 |
|---------|-----------|------|
| 简洁封面 | 1-2 个 | 主标题 + 副标题为主，图标点缀 |
| 技术封面 | 2-3 个 | 展示技术栈或架构概念 |
| 信息图 | 3-5 个 | 每个图标对应一个信息块 |

---

### 尺寸适配

**原始尺寸**：Iconify 返回的 SVG 原始尺寸通常为 24-48px。

**渲染尺寸**：根据画布大小调整，使用 `scale()` 变换。

| 画布尺寸 | 装饰图标 | 主视觉图标 | 示例场景 |
|---------|---------|-----------|---------|
| 900×383（微信公众号） | 40-80px | 80-120px | 博客封面 |
| 1200×630（Open Graph） | 60-100px | 100-150px | 社交分享 |
| 1920×1080（16:9） | 80-120px | 120-180px | 演示文稿 |
| 1080×1080（正方形） | 60-100px | 100-140px | Instagram |

**缩放示例**：
```xml
<!-- 原始 24px，放大 3 倍 → 72px -->
<g transform="scale(3)">
  <path d="..." fill="#58A6FF"/>
</g>
```

---

### 透明度建议

**核心原则**：图标是装饰，不是主角。文字和主要视觉元素必须保持清晰可读。

| 角色 | 透明度 | 用途 |
|-----|--------|------|
| 背景装饰 | `opacity="0.15-0.3"` | 角落点缀，不抢夺主标题注意力 |
| 前景辅助 | `opacity="0.4-0.6"` | 与文字呼应，增强主题感 |
| 高亮强调 | `opacity="0.7-0.9"` | 仅用于强调特定元素（慎用） |

**示例**：
```xml
<!-- 背景装饰：低透明度 -->
<g transform="translate(50, 50) scale(3)" opacity="0.2">
  <path d="..." fill="#4ec9b0"/>
</g>

<!-- 前景辅助：中等透明度 -->
<g transform="translate(700, 300) scale(2)" opacity="0.5">
  <path d="..." fill="#569cd6"/>
</g>
```

---

### 位置建议

**核心原则**：图标不应遮挡文字，应与主要视觉元素保持适当距离。

**推荐位置**：
- **边角装饰**：左上角、右下角，与主标题保持 60-100px 距离
- **对称布局**：左右各一个图标，形成视觉平衡
- **底部支撑**：在文字下方放置图标，作为视觉基座

**避免区域**：
- ❌ 文字正上方（会遮挡标题）
- ❌ 文字正下方（会干扰阅读）
- ❌ 画面中心（会与主视觉冲突）

**安全距离参考**（900×383 画布）：
```
┌─────────────────────────────────┐
│  [图标]  ← 距边缘 40-60px      │
│                                 │
│     主标题（居中或偏左）         │
│     副标题                      │
│                                 │
│              [图标]  ← 右下角   │
└─────────────────────────────────┘
```

---

### 风格一致性原则

**关键**：图标风格必须与整体设计协调，否则融合会显得突兀。

| 设计风格 | 推荐图标集 | 原因 |
|---------|-----------|------|
| 技术/CLI 封面 | `codicon` + `mdi` | VS Code 风格，程序员熟悉 |
| 简约现代 | `lucide` + `heroicons` | 线条简洁，不喧宾夺主 |
| 商务/专业 | `carbon` + `fluent` | IBM/微软设计系统，稳重 |
| 活泼/社交 | `mdi` + `ph` | 色彩丰富，辨识度高 |
| 暗色主题 | `lucide`（浅色图标） | 线条图标在暗背景上更清晰 |

**融合规则**：
1. 图标颜色必须与配色方案一致（不使用图标默认颜色）
2. 图标尺寸与文字协调（参见"尺寸适配"章节）
3. 图标位置不遮挡文字（参见"位置建议"章节）
4. 同一设计中使用同一图标集的图标（不混搭）

---

### Agent 使用示例

#### 示例 1：技术博客封面（简洁版）

**场景**：为"分层分包架构"文章生成微信公众号封面（900×383），使用操作系统主题。

**步骤**：

```
1. 搜索图标（选择 2 个）
   - layers（分层）→ codicon:layers
   - server（服务器）→ codicon:server

2. 获取 SVG JSON 数据
   GET https://api.iconify.design/codicon.json?icons=layers,server

3. 提取 path 数据，调整颜色和尺寸
   - layers：#4ec9b0，scale(3)，opacity=0.2，左上角
   - server：#569cd6，scale(2)，opacity=0.3，右下角
```

**完整 SVG 代码**：
```xml
<svg width="900" height="383" viewBox="0 0 900 383" xmlns="http://www.w3.org/2000/svg">
  <!-- 背景 -->
  <rect width="900" height="383" fill="#1e1e1e"/>
  
  <!-- 图标 1: layers（左上角装饰） -->
  <g transform="translate(50, 40) scale(3)" opacity="0.2">
    <g fill="#4ec9b0">
      <path d="M8 9c-.181 0-.357-.05-.514-.142l-5-3.001A1 1 0 0 1 2 5c0-.35.187-.68.486-.858l5-3a1.01 1.01 0 0 1 1.031.003l4.996 2.998c.3.18.486.508.486.857s-.187.679-.486.858l-5 2.999A1 1 0 0 1 8 9m0-7L3 5l5 3l5-3z"/>
      <path d="M2.146 6.987L8 10.5l5.854-3.513c.092.154.146.33.146.513c0 .35-.186.678-.486.858l-5 3a1 1 0 0 1-1.028 0l-5-3A1 1 0 0 1 2 7.5c0-.184.054-.36.146-.513"/>
      <path d="M2.146 9.487L8 13l5.854-3.513c.092.154.146.33.146.513c0 .35-.186.678-.486.858l-5 3a1 1 0 0 1-1.028 0l-5-3A1 1 0 0 1 2 10c0-.184.054-.36.146-.513"/>
    </g>
  </g>
  
  <!-- 主标题 -->
  <text x="120" y="200" font-family="'Cascadia Code', 'Consolas', monospace" font-size="64" font-weight="bold" fill="#ffffff">
    分层分包架构
  </text>
  
  <!-- 图标 2: server（右下角装饰） -->
  <g transform="translate(780, 280) scale(2)" opacity="0.3">
    <path fill="#569cd6" d="M12 11.5a.5.5 0 1 1-1 0a.5.5 0 0 1 1 0M11.5 8a.5.5 0 1 0 0-1a.5.5 0 0 0 0 1M14 4.5c-.001.37-.14.727-.39 1c.25.273.389.63.39 1v2c-.001.37-.14.727-.39 1c.25.273.389.63.39 1v2a1.5 1.5 0 0 1-1.5 1.5h-9A1.5 1.5 0 0 1 2 12.5v-2c.001-.37.14-.727.39-1a1.5 1.5 0 0 1-.39-1v-2c.001-.37.14-.727.39-1a1.5 1.5 0 0 1-.39-1v-2A1.5 1.5 0 0 1 3.5 1h9A1.5 1.5 0 0 1 14 2.5zm-11 0a.5.5 0 0 0 .5.5h9a.5.5 0 0 0 .5-.5v-2a.5.5 0 0 0-.5-.5h-9a.5.5 0 0 0-.5.5zM12.5 6h-9a.5.5 0 0 0-.5.5v2a.5.5 0 0 0 .5.5h9a.5.5 0 0 0 .5-.5v-2a.5.5 0 0 0-.5-.5m.5 4.5a.5.5 0 0 0-.5-.5h-9a.5.5 0 0 0-.5.5v2a.5.5 0 0 0 .5.5h9a.5.5 0 0 0 .5-.5zM11.5 4a.5.5 0 1 0 0-1a.5.5 0 0 0 0 1"/>
  </g>
</svg>
```

#### 示例 2：产品发布封面（信息图版）

**场景**：为"新功能发布"生成 1200×630 社交分享封面，展示 3 个核心功能。

**步骤**：

```
1. 搜索图标（选择 3 个）
   - rocket（发布）→ mdi:rocket-launch
   - lightning（性能）→ mdi:lightning-bolt
   - shield（安全）→ mdi:shield-check

2. 获取 SVG JSON 数据
   GET https://api.iconify.design/mdi.json?icons=rocket-launch,lightning-bolt,shield-check

3. 提取 path 数据，调整颜色和尺寸
   - 每个图标：scale(3.5)，opacity=0.8，与对应功能文字配对
   - 水平排列，间距 200px
```

**布局示意**：
```
┌──────────────────────────────────────┐
│                                      │
│     新功能发布                        │
│                                      │
│  🚀          ⚡          🛡️          │
│  极速启动     性能翻倍    安全防护    │
│                                      │
└──────────────────────────────────────┘
```

---

### 注意事项

1. **不要直接粘贴完整 SVG 标签**：Iconify 返回的 SVG 包含 `<svg>` 根标签，直接嵌入会与外层 SVG 冲突。应提取内部的 `<path>`、`<g>` 等元素。

2. **颜色覆盖**：Iconify 返回的 SVG 可能包含 `fill="currentColor"` 或默认颜色，需要在嵌入时显式指定颜色（如 `fill="#58A6FF"`）。

3. **性能考虑**：避免在一个设计中使用过多图标（>5 个），会增加 SVG 文件大小和渲染负担。

4. **版权合规**：Iconify 图标集中的图标遵循各自的许可证（通常为 MIT、CC BY 4.0），商用前请确认。
